---
import '@fontsource-variable/inter';

// 'DM Sans'
// Nunito
// Dosis
// Outfit
// Roboto
// Literata
// 'IBM Plex Sans'
// Karla
// Poppins
// 'Fira Sans'
// 'Libre Franklin'
// Inconsolata
// Raleway
// Oswald
// 'Space Grotesk'
// Urbanist
---

<style is:inline>
 
 .form-container {
  display: flex;
  height: 100vh;
  justify-content: center;
  align-items: center;
  background: #f3f7fc; /* Softer, more pleasant background color */
  padding: 2rem; /* Ensures some padding on smaller devices */
}

form {
  background: #ffffff;
  display: flex;
  flex-direction: column;
  padding: 2rem;
  width: 100%;
  max-width: 600px;
  border-radius: 20px;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1); /* Enhanced shadow for floating effect */
  transition: transform 0.3s ease, box-shadow 0.3s ease; /* Smooth transition for hover effect */
}

form:hover {
  transform: translateY(-5px);
  box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15); /* Hover effect to draw attention */
}

form h3 {
  color: #333; /* Darker color for better readability */
  font-weight: 800;
  margin-bottom: 20px;
  font-size: 1.5rem; /* Slightly larger for readability */
  text-align: center;
}

form input, form textarea {
  border: 1px solid #e2e8f0; /* Soft border for clarity */
  margin: 15px 0; /* Increased spacing */
  padding: 15px;
  outline: none;
  background: #f5f5f5;
  font-size: 16px;
  border-radius: 10px; /* Softer corners */
  transition: border-color 0.3s ease, box-shadow 0.3s ease; /* Smooth transitions */
}

form input:focus, form textarea:focus {
  border-color: #7b80e4; /* Accent color for focus state */
  box-shadow: 0 0 10px rgba(123, 128, 228, 0.2); /* Soft focus glow */
  background-color: #fff; /* Clearer background when focused */
}

form button {
  padding: 15px;
  background: #7b80e4;
  color: #fff;
  font-size: 18px;
  border: 0;
  outline: none;
  cursor: pointer;
  width: 100%; /* Full width button */
  margin: 20px 0 0;
  border-radius: 20px;
  transition: background-color 0.3s ease, transform 0.3s ease; /* Smooth hover transition */
}

form button:hover {
  background: #6f74dc; /* Darker shade on hover */
  transform: scale(1.05); /* Slightly larger button on hover */
}

form button:active {
  background: #5e63c9; /* Even darker shade on click */
  transform: scale(0.98); /* Button shrink on click */
}

@media (max-width: 768px) {
  form {
    padding: 1.5rem; /* Reduce padding on smaller devices */
  }
  form button {
    width: 100%; /* Ensure button takes full width on mobile */
  }
}


  :root {
    --aw-font-sans: 'InterVariable';
    --aw-font-serif: 'InterVariable';
    --aw-font-heading: 'InterVariable';

    --aw-color-primary: rgb(1 97 239);
    --aw-color-secondary: rgb(1 84 207);
    --aw-color-accent: rgb(109 40 217);

    --aw-color-text-heading: rgb(0 0 0);
    --aw-color-text-default: rgb(16 16 16);
    --aw-color-text-muted: rgb(16 16 16 / 66%);
    --aw-color-bg-page: rgb(255 255 255);

    --aw-color-bg-page-dark: rgb(3 6 32);

    ::selection {
      background-color: lavender;
    }
  }

  .dark {
    --aw-font-sans: 'InterVariable';
    --aw-font-serif: 'InterVariable';
    --aw-font-heading: 'InterVariable';

    --aw-color-primary: rgb(1 97 239);
    --aw-color-secondary: rgb(1 84 207);
    --aw-color-accent: rgb(109 40 217);

    --aw-color-text-heading: rgb(247, 248, 248);
    --aw-color-text-default: rgb(229 236 246);
    --aw-color-text-muted: rgb(229 236 246 / 66%);
    --aw-color-bg-page: rgb(3 6 32);

    ::selection {
      background-color: black;
      color: snow;
    }
  }
  
</style>
